---
Title: Release v0.3.0
Author: Johann Rudi
Co-Authored-By: Claude Opus 5
Date: 2026-03-18
tags:
  - release
  - changelog
---

# Release v0.3.0

## Summary

This version is the first published to PyPI, and it is the release in which the toolkit acquired its quality gate. Continuous integration now runs formatting, compilation, tests, and the `basedpyright` linter on every push, and the codebase was reviewed module by module to satisfy it: `nets`, `opt`, `loss`, and `metrics` all gained docstrings and type declarations, and the type definitions that networks share moved into `dlk/nets/utils.py`.

Two changes are visible in imports. The configuration module `config` became `dlk.mgmt`, which now loads and saves parameter files, adds its arguments to an `argparse` parser, and owns logging setup. `dlk.mode` is new and carries the execution modes an application selects between, such as train, predict, and eval.

`dlk.metrics` gained the Hellinger distance in several forms: histogram-based, kernel-density-based through the optional `torchkde` package, and a scale-invariant variant. The package no longer depends on numpy, since torch does that work, and the minimum Python version rose to 3.11 so that `tomllib` is available from the standard library.

## Changelog

### Features

- 41ee1ce metrics: Hellinger distance from histograms, from a kernel density estimate via `torchkde`, and in a scale-invariant variant
- e3a2fda metrics: grid evaluation of a kernel density estimate of samples
- 32c0e4c mgmt: `dlk.mode` for handling execution modes such as train, predict, and eval
- 4156322 mgmt: loading and saving of configuration files, with unit tests
- 58017ca mgmt: a function that adds the package's arguments to an `argparse` instance

### Bug fixes

- 1a78eba build: add the missing `pyyaml` dependency
- db0114f ci: keep tests passing when the optional `torchkde` import is unavailable
- 6349bc0 mgmt: default arguments and help text for the argument parser

### Refactorings

- d77a116 nets, opt, loss, metrics: code review across the package, adding docstrings and type declarations
- 31d3d3c nets: consolidate the shared type definitions in `utils.py` and tighten the declarations
- ee3999a nets: make `set_init_parameters` type-safe without adding complexity
- 18a4126 mgmt: rename the `config` directory to `mgmt` and shorten the function names
- 7d1bceb metrics: replace numpy with torch, removing the numpy dependency
- 547be63 opt: use `pathlib` in place of `os`
- 9030217 build: raise the minimum Python version to 3.11 for `tomllib`

### Documentation

- 181f49d docs: README describing the package in more detail
- 4862785 docs: update the feature documents and rename the folder to `features`
- 7870ca0 docs: shorten the AGENTS file

### Build and CI

- f3e5283 ci: workflows for formatting, compilation, tests, and linting, with status badges
- 766ea84 ci: publish to PyPI when a tag is pushed
- 8480d5e build: set up `bumpver` for version bumps
- f299965 build: a Makefile for the commands used across the codebase, extended to the tests directory
- 9a5f0a4 build: add `basedpyright` to the development install and a lint rule to the Makefile
- cc46edf build: review and improve the configuration in `pyproject.toml`
